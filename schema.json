{ 
    "pipeline": "Irods extractor",
    "params": {
        "Input_parameters": {
            "header": {
                "title": "Sequencing reads",
                "subtext": "There are two ways of providing input reads, which can be combined\n      1) through direct input of compressed fastq sequence reads files. This kind of input is passed by specifying the paths to the\n      read files via a manifest listing the pair of read files pertaing to a sample, one per row.",
            },
            "manifest_of_reads": {
                "default": "false",
                "help_text": "Manifest containing per-sample paths to .fastq.gz files (optional)"
            },
            "header_2": {
                "subtext": "2) through specification of data to be downloaded from iRODS. Each sample is defined by a combination of study, run, lane and plex ids\n      (these ids correspond to the reference of the sequencing experiment). Run, lane and plex ids are not mandatory: when provided, these\n      parameters gradually restrict of files to be downloaded; when ommitted, samples for all possible values are retrieved.\n      This information can be provided via a combination of workflow parameters passed on through command line options: --studyid, --runid,\n      --laneid and --plexid; this defines a single sequencing dataset based on a combination of study, run, lane and plex ids.",
            },
            "studyid": {
                "default": -1,
                "help_text": "Sequencing Study ID"
            },
            "runid": {
                "default": -1,
                "help_text": "Sequencing Run ID"
            },
            "laneid": {
                "default": -1,
                "help_text": "Sequencing Lane ID"
            },
            "plexid": {
                "default": -1,
                "help_text": "Sequencing Plex ID"
            },
            "header_3": {
                "subtext": "Alternatively, the user can provide a manifest listing a batch of such combinations."
            },
            "manifest_of_lanes": {
                "default": "false",
                "help_text": "Path to a manifest of search terms as specified above"
            },
            "header_4": {
                "title": "Reference input",
                "subtext": "Currently only input through path to file is supported"
            },
            "reference": {
                "default": "none",
                "help_text": "Path to a reference in fasta format"
            }
        },
        "Procesing options": {
            "header": {
                "title": "iRODS extractor options:", 
                "subtext": ""
            },
            "cleanup_intermediate_files_irods_extractor": {
                "default": "false",
                "help_text": "delete intermediate files from IRODS"
            },
            "header_2": {
                "title": "Strain-mapper read mapping options:",
                "subtext": ""
            },
            "only_report_alts": {
                "default": "true",
                "help_text": "When included this flag reports only ALT variants in the VCF output"
            },
            "VCF_filters": {
                "default": "QUAL>=50 & MIN(DP)>=8 & ((ALT!=\".\" & DP4[2]>3 & DP4[3]>3) | (ALT=\".\" & DP4[0]>3 & DP4[1]>3))",
                "help_text": "Parameters for filtering variants in VCF file. Default is to removing records with a quality score below 50 and also requiring 3 reads\n             from each strand with overall greater than 8."
            },
            "skip_filtering": {
                "default": "false",
                "help_text": "Do not filter variants called using `bcftools call` based on metrics defined with --VCF_filters."
            },
            "keep_raw_vcf": {
                "default": "false",
                "help_text": "Save the unfiltered VCF file i.e. direct output of `bcftools call`; can be combined with --only_report_alts=false\n            to report all (unfiltered, REF and ALT) variants; only relevant when --skip_filtering=false;"
            },
            "keep_sorted_bam": { 
                "default": "false",
                "help_text": "Save the mapping file (sorted BAM)"
            },
            "save_fastqs": { 
                "default": "false",
                "help_text": "Save the fastq read files directly into the results directory (incompatible with --save_nested_fastqs)"
            },
            "save_nested_fastqs": {
                "default": "false",
                "help_text": "Save the fastq read files within sample specific subdirectories (incompatible with --save_fastqs)"
            }
        }
    },
    "overwrite_param" : {
        "irods_params": {
        }
    }
}